import React from "react";
import { ContentSnapWrapper, ContentWrapper } from "containers/app.style";

const SnapWrapper = ({ children }) => {
  return <ContentSnapWrapper>{children}</ContentSnapWrapper>;
};

export default SnapWrapper;
