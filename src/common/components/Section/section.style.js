import styled from "styled-components";
import {
  space,
  lineHeight,
  size,
  color,
  borderRadius,
  display,
  textAlign,
} from "styled-system";

const SectionStyle = styled("section")`
  ${space}
  ${color}
  ${size}
  ${lineHeight}
  ${borderRadius}
  ${display}
  ${textAlign}
`;

export default SectionStyle;
